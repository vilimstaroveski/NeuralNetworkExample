Data copied from coinmarketcap
Note: data is sorted from newest to oldest (needs to be reversed at some point)

Add new data points, by adding coinmarketcap data to the [coin].txt file, AT START OF FILE (file is reversed)

At start, network should be run with empty LOG.

